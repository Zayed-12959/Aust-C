WAP to input side of a square and calculate the area.

#include<stdio.h>

int power(int base, int exp){
    int result=1;
    for(int i=1; i<=exp; i++){
        result = result*base;
    }
return result;
}

int main(){

    int side;
    printf("Enter side if a square: ");
    scanf("%d", &side);

    printf("Area: %d", power(side,2));

return 0;
}
