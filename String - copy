WAP to copy a string into another string


#include<stdio.h>

int main(){

    char str1[100];
    char str2[100];
    int i,j;

    printf("Enter a string:");
    gets(str1);

    for(i=0, j=0; str1[i]!='\0'; i++, j++){
        str2[j] = str1[i];
    }
    str2[j] = '\0';

    printf("%s", str2);

return 0;
}
