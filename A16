WAP to input marks of a student and print the result (pass/fail) using conditional operator.

#include<stdio.h>

int main(){

    int marks;
    printf("Enter marks:");
    scanf("%d", &marks);

    if(marks>=40){
        printf("Pass");
    }
    else if(marks<40){
        printf("Fail");
    }

return 0;
}
