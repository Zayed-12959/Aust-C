 WAP to input the name and age of a person and print the name as many times as age.

#include<stdio.h>

int main(){

    char name[100];
    int age;

    printf("Enter name:");
    gets(name);
    printf("Enter age:");
    scanf("%d", &age);

    for(int i=1; i<=age; i++){
        printf("%s\n", name);
    }

return 0;
}
