WAP to input two numbers and print the greatest using conditional operator.

#include<stdio.h>

int main(){

    int a,b;
    printf("Number 1:");
    scanf("%d", &a);
    printf("Number 2:");
    scanf("%d", &b);

    if(a==b){
        printf("Equal");
    }
    else if(a>b){
        printf("Greatest: %d", a);
    }
    else{
        printf("Greatest: %d", b);
    }

return 0;
}
