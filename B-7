 WAP to input the name and age of a person and display “CHILD” or “TEENAGER” according to the age.

#include<stdio.h>

int main(){

    int age;

    printf("Enter age:");
    scanf("%d", &age);

    if(age>=6 && age<=12){
        printf("CHILD");
    }
    else if(age>=13 && age<=19){
        printf("TEENAGER");
    }
    else{
        printf("invalid age input");
    }

return 0;
}
