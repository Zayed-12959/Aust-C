WAP to print all the numbers falling between 2 numbers entered by the user.

#include<stdio.h>

int main(){

    int start,end;

    printf("Enter start and end of the range:");
    scanf("%d-%d", &start, &end);

    for(int i=start; i<=end; i++){
        printf("%d ", i);
    }

return 0;
}
